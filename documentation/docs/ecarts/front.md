---
title: Écarts assumés — tickets FRONT
description: Les écarts entre les tickets FRONT et ce qui a été livré, avec leur raison.
---

# Écarts assumés — tickets FRONT

Chaque ticket FRONT consigne ici les écarts entre son énoncé et ce qui a été livré — le principe de
ce registre est expliqué sur [la page d'index](./index.md).

## Écarts assumés avec le ticket FRONT-01

| Écart                                                               | Raison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Pas de `tailwind.config.ts`                                         | Le ticket demande un fichier qui n'existe plus : Tailwind v4 n'a pas de configuration JavaScript. Le thème **est** le CSS partagé, et le `content` d'autrefois s'écrit `@source` — arbitrage déjà rendu en SHARED-02.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `@repo/api-client` absent des dépendances et de `transpilePackages` | Le package est l'objet de SHARED-03 et n'existe pas : une dépendance `workspace:*` vers un package inexistant fait échouer `pnpm install`. Les deux lignes reviendront au ticket qui le crée. **SHARED-03 a levé l'écart** : le package existe, l'application le déclare en `workspace:*` et le transpile.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `@repo/typescript-config` et `@repo/tailwind-config`                | Le ticket écrit `@repo/config-typescript` et `@repo/config-tailwind`. Ce sont les noms des dossiers, pas ceux des packages — même écart qu'en SHARED-02.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `outputFileTracingRoot` ajouté à `next.config.ts`                   | Non demandé. Sans lui la sortie `standalone` est tracée depuis le dossier de l'application et n'embarque pas les dépendances atteintes par les liens symboliques pnpm : elle se construit sans erreur puis échoue au démarrage, ce qui rendrait le critère d'acceptation faux tout en le laissant passer.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `agentRules: false`                                                 | Next 16 dépose de lui-même un `AGENTS.md` et un `CLAUDE.md` dans l'application à chaque `next dev`. Le dépôt n'a aucun fichier de ce genre ; les garder imposerait soit une modification non commitée en permanence, soit de la prose générée à relire à chaque PR. La documentation du dépôt reste ce README et `documentation/`.                                                                                                                                                                                                                                                                                                                                                                                          |
| Deux renvois de SETUP-03 levés, deux conservés                      | `REACT_VERSION` est figée — les applications épinglent react 19.2.8 — et le résolveur TypeScript est posé, ce qui active `import-x/no-unresolved` et `import-x/no-cycle`. **SETUP-06 et SETUP-07 ont levé l'écart** — `base.js` applique désormais `recommendedTypeChecked` avec le service de projet, et `react.js` branche `eslint-plugin-jsx-a11y-x`, la variante retenue et motivée au tableau SETUP-07. À la livraison de FRONT-01, `tseslint.configs.recommendedTypeChecked` et `eslint-plugin-jsx-a11y` restaient hors du socle : le premier change le coût de chaque `pnpm lint` et mérite sa propre mesure, le second est un vrai sujet que le commentaire d'origine rattache au parcours de prise de rendez-vous. |
| Réordonnancement des imports `@repo/*`                              | Effet du résolveur, pas une préférence : sans lui `@repo/eslint-config/base` était classé comme un paquet externe, et le `pathGroups` de `rules.js` qui le range en « interne » restait sans effet. Un seul fichier du dépôt était concerné, `eslint.config.mjs`, corrigé automatiquement.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `typescript@6.0.3`, et non le motif d'alias TypeScript 7            | SETUP-03 renvoyait ce choix ici. TypeScript 7 est bien la version `latest` sur npm, mais ne livre toujours pas d'API compilateur — annoncée pour la 7.1, encore en pré-publication — ce qui bloque typescript-eslint ; et le paquet `@typescript/typescript6` que le motif utilise plafonne à 6.0.2, une version derrière celle du dépôt. L'adopter aujourd'hui désalignerait le dépôt pour un gain nul.                                                                                                                                                                                                                                                                                                                    |
| `lucide-react` déclarée par l'application                           | `@repo/ui` la porte déjà, mais le `node_modules` strict de pnpm interdit d'importer ce qu'on ne déclare pas — et la bascule de thème utilise ses icônes directement. Même version que le package, pour que pnpm n'en installe qu'une.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Page d'accueil plus fournie que « minimale »                        | Le ticket demande d'afficher un composant. Un bouton seul prouve la transpilation, mais ni le thème sombre, ni la police, ni la non-purge des classes de l'application. Chaque élément de la page atteste un maillon précis, et sa disparition désignerait la pièce cassée.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Textes d'interface accentués                                        | Le reste du dépôt écrit son français sans accents hors des `.md`. La règle ne peut pas s'étendre à ce qui s'affiche : « cliniques veterinaires » sur l'écran d'un cabinet serait une faute, pas une convention. Commentaires et messages de commit restent sans accents.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

:::note Critères hérités désormais vérifiés
Les deux critères que SHARED-01 et SHARED-02 avaient dû laisser en suspens faute d'application —
« les trois applications affichent un composant » et « une modification du preset se répercute
visuellement » — sont désormais vérifiables, et vérifiés sur `frontend-professional`. Les deux
tableaux de la page [SHARED](./shared.md) gardent leur rédaction d'origine : ils décrivent l'état
du dépôt au moment de leur ticket, pas celui d'aujourd'hui.
:::

## Écarts assumés avec le ticket FRONT-02

| Écart                                                                      | Raison                                                                                                                                                                                                                                                                                                                                                         |
| -------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ThemeToggle` remonté dans `@repo/ui`, et `frontend-professional` retouché | Le ticket l'impose : « tout élément visuel commun doit vivre dans `@repo/ui` », et la bascule de thème est le premier composant que deux applications se partagent. Une application déjà livrée est donc modifiée par le ticket d'une autre — c'est le prix de la règle, et il valait mieux le payer sur une copie que sur trois.                              |
| `lucide-react` retiré de `frontend-professional`                           | Conséquence de la ligne précédente : l'application n'importe plus aucune icône directement, seul `@repo/ui` le fait. La ligne du tableau FRONT-01 qui justifiait cette dépendance n'est pas corrigée — elle décrivait l'état du dépôt à sa date.                                                                                                               |
| Un `.gitkeep` dans les `components/` des deux applications                 | Chaque `app/globals.css` porte `@source '../components/**/*.{ts,tsx}'`. Le dossier de `frontend-professional` s'est vidé, celui de `frontend-individual` naît vide : garder les deux présents évite de faire diverger trois feuilles de style pour une raison passagère.                                                                                       |
| `app/site-url.ts` ajouté au périmètre                                      | Trois fichiers doivent s'accorder sur la même URL canonique — `metadataBase`, `robots.ts`, `sitemap.ts`. Trois copies divergeraient au premier changement de domaine, et la divergence serait silencieuse : un sitemap qui annonce un autre hôte que les balises canoniques n'échoue pas, il est ignoré.                                                       |
| Variable `SITE_URL` ajoutée hors de la liste du ticket                     | Le ticket demande des métadonnées Open Graph et un sitemap, qui exigent tous deux une URL absolue. Sans préfixe `NEXT_PUBLIC_` : ses consommateurs tournent au build ou sur le serveur, jamais dans le navigateur — même raisonnement que pour `API_INTERNAL_URL`.                                                                                             |
| Génération statique obtenue sans `force-static`                            | Le ticket demande de « l'activer lorsque c'est pertinent ». Rien à activer : sans API dynamique, Next prérend déjà tout au build. L'écrire quand même banaliserait la directive, alors que sa valeur tient à sa rareté.                                                                                                                                        |
| Ni image Open Graph ni manifest                                            | Périmètre arbitré à l'ouverture du ticket. Les deux réclament des visuels que le dépôt n'a pas, et une carte de partage annonçant une image absente vaut moins qu'une carte sobre. Détaillé dans [Le volet SEO de `frontend-individual`](../frontend/les-trois-applications.md#le-volet-seo-de-frontend-individual).                                           |
| Deuxième carte sur la page d'accueil                                       | Le ticket ne décrit pas la page. Celle du patron atteste le câblage du monorepo ; il manquait de quoi attester le volet propre à cette application-ci, d'où deux liens vers `/robots.txt` et `/sitemap.xml` qui se vérifient d'un coup d'œil.                                                                                                                  |
| Pas de `tailwind.config.ts`                                                | Le ticket demande de reprendre « le tailwind.config de FRONT-01 », qui n'existe pas : Tailwind v4 n'a plus de configuration JavaScript. Même arbitrage qu'en SHARED-02 et FRONT-01 — le thème **est** le CSS partagé.                                                                                                                                          |
| `@repo/api-client` absent des dépendances et de `transpilePackages`        | Le package relève de SHARED-03 et n'existe pas : une dépendance `workspace:*` vers un package inexistant fait échouer `pnpm install`. Même écart qu'en FRONT-01, et il se lèvera au même moment pour les trois applications. **SHARED-03 a levé l'écart**, pour les trois d'un coup — `frontend-admin` compris, dont le tableau n'avait pas eu à le consigner. |
| `@repo/typescript-config` et `@repo/tailwind-config`                       | Le ticket écrit `@repo/config-typescript` et `@repo/config-tailwind` : ce sont les noms des dossiers, pas ceux des packages. Même écart qu'en SHARED-02 et FRONT-01.                                                                                                                                                                                           |
| Textes d'interface accentués                                               | Le dépôt écrit son français sans accents hors des `.md`, mais la règle ne peut pas s'étendre à ce qui s'affiche — et moins encore ici : ces textes sont ceux que liront les propriétaires d'animaux, et les moteurs. Commentaires et messages de commit restent sans accents.                                                                                  |

:::note Déplacer plutôt que copier
Le critère « aucun composant n'est dupliqué depuis frontend-professional » a été tenu par la seule
voie qui le rende vrai durablement : déplacer le composant partagé plutôt que le copier. C'est
pourquoi ce ticket touche `frontend-professional` et `@repo/ui` en plus de son propre dossier —
les deux applications ont été relancées et comparées après le déplacement, celle de FRONT-01 est
inchangée à l'écran.
:::

## Écarts assumés avec le ticket FRONT-03

| Écart                                                        | Raison                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `proxy.ts` et `app/(auth)/login/`, du périmètre de FRONT-07  | « Redirection vers la page de connexion par défaut » ne s'implémente pas sans eux. Les chemins sont ceux que FRONT-07 annonce, pour qu'il remplace une fonction au lieu de re-router l'application ; rien n'y vérifie de jeton.                                            |
| `proxy.ts` et non `middleware.ts`                            | Next 16 a renommé la convention. L'ancien nom fonctionne encore mais fait avertir **chaque build**, exactement ce que FRONT-01 a refusé en désactivant `agentRules`. FRONT-07 écrit `middleware.ts` dans son périmètre : c'est le même fichier sous son ancien nom.        |
| `@tanstack/react-table` ajouté à `@repo/ui`                  | Conséquence directe du « sinon créer une extension dans le package partagé » du ticket, la vérification ayant conclu par la négative — `table.tsx` est purement présentationnel, et le registre shadcn n'a pas de `data-table` à livrer.                                   |
| `Pagination` du registre écarté au profit de boutons         | Envisagé, puis rejeté après lecture : ce composant est fait de **liens**, donc destiné à une pagination portée par l'URL. La page d'une `DataTable` est un état de la table, sans adresse propre — et un `<a>` sans `href` n'est ni focalisable ni actionnable au clavier. |
| `lucide-react` déclarée par l'application                    | `@repo/ui` la porte déjà, mais le `node_modules` strict de pnpm interdit d'importer ce qu'on ne déclare pas : les icônes de la navigation sont une donnée de l'application, pas du package. Même version que lui, pour que pnpm n'en installe qu'une.                      |
| `force-dynamic` sur `(protected)` seulement                  | Le ticket demande qu'il n'y ait pas de rendu statique public. Le contenu privé est dans ce groupe ; la page de connexion n'a rien à cacher, et l'y soumettre l'aurait ralentie sans rien protéger.                                                                         |
| `TooltipProvider` monté dans l'application                   | Les info-bulles de la barre repliée en dépendent, et ce fournisseur n'a de sens que là où des info-bulles existent. Le placer dans le `ThemeProvider` partagé l'aurait imposé aux deux autres applications sans raison.                                                    |
| Deux sections de démonstration (`cliniques`, `utilisateurs`) | Un fil d'Ariane qui n'affiche jamais qu'un seul niveau ne prouve rien, et le rôle lu par la garde resterait une intention sans écran pour l'afficher.                                                                                                                      |
| Données de la table écrites en dur                           | Ni API ni cache avant SHARED-03 et FRONT-04. Ce que ces lignes servent à montrer, c'est que l'extension du package trie, filtre et pagine — la même logique que les pages de démonstration de FRONT-01 et FRONT-02.                                                        |

## Écarts assumés avec le ticket FRONT-04

| Écart                                                                                                | Raison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Pas de `app/providers.tsx` dans les trois applications                                               | `QueryProvider` porte déjà `'use client'` : un layout resté composant serveur le monte directement, comme il monte le `ThemeProvider` de `@repo/ui` depuis FRONT-01. Un fichier intermédiaire par application aurait ouvert un **second** endroit où l'ordre des fournisseurs se décide — trois occasions de diverger, quand le ticket refuse par ailleurs « trois configurations divergentes ». FRONT-03 avait déjà tranché ainsi en montant `TooltipProvider` dans le layout qui en a besoin.                                                                                                                                                                        |
| Cinq fichiers écrits à la main, là où le ticket en nommait deux                                      | Même règle qu'en SHARED-03 : une raison de changer par fichier. `query-keys.ts` change avec la convention de tenance, `query-client.ts` avec la politique de cache, `unauthorized.ts` avec le contrat d'authentification, `query-server.ts` avec l'hydratation, `query-provider.tsx` avec le montage React. Et une raison mécanique : `tsconfig.verify.json` n'inclut pas les `.tsx`, donc tout ce qui doit rester vérifiable par `make verify-api-client` devait sortir du composant.                                                                                                                                                                                 |
| Un singleton de navigateur derrière l'initialiseur `useState`                                        | Le ticket demande le `useState`, et il est là — mais seul, il ne tient pas : React jette l'état d'un composant qui suspend avant d'avoir commité et rejoue l'initialiseur à chaque tentative. Mesuré sous une sonde suspendue sans frontière `<Suspense>` : **deux** `QueryClient` distincts sans le singleton, **un** avec, et le client commité est neuf — cache vide, requêtes en vol perdues. La variable de module est **gardée par l'environnement** : côté serveur la branche n'est jamais atteinte, chaque appel fabrique un client neuf, et l'avertissement de `runtime.ts` reste tenu.                                                                       |
| Le groupe actif est un **argument obligatoire**, pas un contexte React                               | Rien ne sait dire quel est le groupe actif avant FRONT-07. Un `useRequestScope()` livré ici aurait été soit un contexte vide rendant `null` — donc une clé de tenance sans groupe, ce que le critère interdit —, soit le contexte d'authentification de FRONT-07 écrit d'avance. L'argument obligatoire est de surcroît ce qui rend la fabrique **pure**, donc exécutable sans React.                                                                                                                                                                                                                                                                                  |
| `clinicId` entre dans la clé, ce que le ticket ne demandait pas                                      | L'[ADR-0012](../adr/0012-perimetre-de-requete.md) fait basculer la clinique côté client, sans réémission de jeton, et le mutator l'envoie par requête. Changer de clinique **dans un même onglet** ne change donc ni la route ni la clé d'Orval, et la seconde afficherait les données de la première pendant tout le `staleTime` — la panne du ticket, un cran plus bas, à l'intérieur d'un périmètre pourtant autorisé. Réservé aux ressources de niveau clinique : l'y mettre partout dupliquerait un cache qui n'en dépend pas.                                                                                                                                    |
| Aucune famille `account`, alors qu'elle avait été écrite                                             | Envisagée, puis **retirée après la revue contradictoire**, qui l'a mise en défaut par la mesure : une clé de compte sans identifiant de compte n'isole rien, et le profil d'Alice restait servi à Bob après une reconnexion dans le même onglet. Rien ne sait dire quel compte est connecté avant FRONT-07 — c'était la seule des quatre familles dont la justesse reposait entièrement sur un ticket qui n'existe pas.                                                                                                                                                                                                                                                |
| Une convention de clé promue en [ADR-0027](../adr/0027-portee-des-cles-de-cache.md)                  | Elle prolonge côté client la frontière de tenance de l'[ADR-0004](../adr/0004-tenance-par-groupe.md) et tranche ce que l'ADR-0012 laissait ouvert. Tous les tickets frontend à venir construiront dessus, et sa violation ne produit ni erreur de compilation ni test rouge : elle produit des données médicales d'un groupe affichées dans un autre.                                                                                                                                                                                                                                                                                                                  |
| Le routage des 401 vit dans `unauthorized.ts`, hors du `QueryProvider`                               | Le module retient une fonction **et un verrou** — un état de module, exactement ce que l'en-tête de `query-client.ts` s'interdit. Le placer dans le composant l'aurait de plus rendu invisible à `make verify-api-client`, et aurait obligé le `src/auth/` de FRONT-07 à importer un `.tsx` depuis un `.ts`, ce que la passe de vérification refuse.                                                                                                                                                                                                                                                                                                                   |
| Les **trois** fonctions de la couture portent la garde d'environnement                               | Poser la garde sur le seul signalement aurait suffi à empêcher le déclenchement, pas le **dépôt** : un module `'use client'` est exécuté au rendu serveur, et un `setUnauthorizedHandler` écrit au niveau d'un module y aurait déposé la fermeture d'un utilisateur dans un état partagé par tout le processus. La revue contradictoire a montré que l'argument « deux fabriques, donc isolation structurelle » était **faux** — React exécute les initialiseurs de `useState` au rendu serveur, mesuré sur `renderToString` : c'est la garde, et elle seule, qui protège.                                                                                             |
| Un second programme de vérification, `scripts/verify-query-keys.ts`, et un script `test`             | Le critère dit « (test de bascule) » et le dépôt n'a aucun runner de test frontend — QA-02 le possède, FRONT-06 en dépend, et tous deux **dépendent de ce ticket**. Le loger dans `make verify-api-client` aurait exigé PostgreSQL, Redis, MinIO et l'API pour comparer deux tableaux. Le programme est hors ligne, sans dépendance et sans compilation ; le script `test` fait mordre `make test-front`, exactement ce que le `Makefile` avait prévu.                                                                                                                                                                                                                 |
| `src/query-keys.ts` n'a **aucun import de valeur**, et cette pureté est une contrainte de conception | C'est elle qui rend le fichier exécutable par `node` sans compilation, donc la bascule prouvable hors ligne. La contrainte est écrite en tête du fichier : un import de **type** y survivrait, un import de **valeur** non — vérifié, `node src/mutator.ts` sort en `ERR_MODULE_NOT_FOUND` sur exactement cette ligne. Le programme de preuve importe par **auto-référence** (`@repo/api-client/query-keys`) et non par chemin relatif, faute d'extension.                                                                                                                                                                                                             |
| `allowImportingTsExtensions` envisagé, puis **retiré après mesure**                                  | La première version écrivait `from '../src/query-keys.ts'`, ce qui exige cette option de compilation. **Orval la lit aussi** : la régénération s'est mise à écrire `from '../../../mutator.ts'` dans tout le code généré, faisant échouer `make generate-api-check` — c'est-à-dire le contrôle anti-dérive de l'[ADR-0007](../adr/0007-client-api-genere-orval.md). L'auto-référence au package obtient le même résultat sans toucher à la configuration.                                                                                                                                                                                                              |
| `useQuery: true` retiré d'`orval.config.ts`, hors du périmètre littéral                              | Mesuré sur un contrat de sonde portant un POST : avec cette ligne, Orval génère **zéro** `useMutation` et sort le POST en `useQuery`, avec sa clé de cache — il partirait au montage et à chaque invalidation. SHARED-03 avait bien vu que le drapeau symétrique cassait les GET, et écrit celui qui casse les autres verbes ; le contrat n'ayant que deux GET, rien ne pouvait le révéler. C'est FRONT-04 qui corrige parce que c'est lui qui pose la politique de mutation — `retry: false`, routage des 401 sur le `MutationCache` : la livrer sans le hook qui la consomme aurait été livrer du code mort. Le retrait ne change pas un octet de la sortie générée. |
| `useSuspenseQuery` maintenu à `false`                                                                | `orval.config.ts` renvoyait nommément la décision à ce ticket. Suspense déplace le chargement dans un `<Suspense>` et l'erreur dans une frontière d'erreur : ce sont les périmètres nommés de FRONT-18a et de FRONT-10, qui deviendraient impossibles à écrire tels qu'ils le sont. Et `useSuspenseQuery` n'accepte pas `enabled`, dont une requête dépendante de la clinique active aura besoin. Le rouvrir ne coûtera qu'un drapeau et une régénération.                                                                                                                                                                                                             |
| `@tanstack/react-query-devtools` en `dependencies`, et déclarée par le seul package                  | `query-provider.tsx` l'importe, et ce fichier est du code de production consommé **en source** par les trois applications : la dépendance est nécessaire au build. Même arbitrage qu'en SHARED-03 pour `@tanstack/react-query`, et même version épinglée — sa `peerDependency` est `^5.101.4`, les deux doivent rester alignées.                                                                                                                                                                                                                                                                                                                                       |
| L'exclusion des Devtools ne vient **pas** de la garde du fournisseur                                 | Mesuré, et le commentaire du fichier corrigé en conséquence : garde retirée, `next build` rejoué, le panneau — quelque 600 Ko de `@tanstack/query-devtools` — reste absent dans les deux cas ; seul l'élément JSX réapparaît, pour 174 octets. C'est le paquet qui s'auto-neutralise, son point d'entrée valant `process.env.NODE_ENV !== 'development' ? () => null : …`. La garde reste — elle retire l'élément de l'arbre au lieu d'y rendre une souche — mais elle n'est pas ce qui tient le critère.                                                                                                                                                              |
| `query-server.ts` protégé par un commentaire, pas par `server-only`                                  | L'en-tête écrit « à n'importer que depuis un composant serveur », et rien ne le tient : un composant client peut l'importer, cela compile. Le rendre mécanique demandait d'ajouter la dépendance `server-only` à un package qui n'en a que trois, pour un chemin qu'aucune page n'emprunte encore. À reprendre par le premier ticket qui préchargera réellement (FRONT-18a).                                                                                                                                                                                                                                                                                           |
| Un composant de démonstration ajouté à `frontend-professional`                                       | Monter un fournisseur que rien ne consomme ne prouve rien : deux exemplaires de `@tanstack/react-query` rendraient le contexte invisible aux hooks **sans la moindre erreur de compilation**, et `make verify-api-client` ne rend aucun hook. Un seul écran, dans l'application dont le layout annonçait ce ticket, et il disparaîtra avec le premier écran métier. Il passe par la fabrique de clés, pour que l'exemple à copier soit celui-là.                                                                                                                                                                                                                       |
| Une étape ajoutée au workflow `api-client.yml`                                                       | Le critère dit « (test de bascule) », et une preuve qui ne tourne que si quelqu'un la tape n'en est pas une : les trois workflows du dépôt ne rejouaient que la génération, les contrats d'architecture et le site. Ce workflow-ci surveille déjà `packages/api-client/**`, installe déjà les dépendances du package, et le programme est hors ligne — il ne demande ni pile ni compilation. L'outillage de CI frontend au sens large reste à QA-01 et QA-02.                                                                                                                                                                                                          |
| Sept commentaires et passages périmés corrigés hors du périmètre littéral                            | Le `Makefile` et sa page annonçaient qu'aucun workspace ne déclarait de script `test` ; `orval.config.ts` parlait de FRONT-04 au futur dans le fichier même que ce ticket édite ; `ecarts/shared.md` écrivait que le `QueryProvider` « vivra » ; `les-trois-applications.md` décrivait un socle commun sans le fournisseur qu'il porte désormais ; `installation.md` disait qu'aucun écran n'appelait l'API. Précédent explicite au registre SHARED-03 : « le laisser intact aurait été une consigne périmée en tête du fichier même qui la rend caduque ».                                                                                                            |
| Textes d'interface accentués                                                                         | Le dépôt écrit son français sans accents hors des `.md`, mais la règle ne s'étend pas à ce qui s'affiche : « Service prêt » sur l'écran d'un cabinet. Même arbitrage qu'en FRONT-01 et FRONT-02 ; commentaires et messages de commit restent sans accents.                                                                                                                                                                                                                                                                                                                                                                                                             |

:::note Ce que ce ticket ne pouvait pas rendre mécanique
La fabrique rend **impossible** de composer une clé de tenance sans groupe — c'est le typage qui
refuse, et il refuse aussi qu'un identifiant de clinique soit re-marqué en groupe. Elle n'empêche pas
d'appeler directement un hook généré, qui se rangerait alors sous la clé nue d'Orval, ni d'enfermer
une clé de portée dans une autre — `publicQueryKey(groupQueryKey(...))` compile. Dans les deux cas la
donnée échappe à la purge de bascule. Les deux parades réellement mécaniques — un sceau vérifié à
l'exécution par `queryKeyHashFn`, la fermeture de `./api/*` dans la carte `exports` — ont été
envisagées puis écartées : aucune couche d'enveloppement n'existe encore (FRONT-09), et l'une comme
l'autre interdirait aujourd'hui tout appel d'API. Le motif complet, et la condition de réouverture,
sont dans l'[ADR-0027](../adr/0027-portee-des-cles-de-cache.md).
:::

## Écarts assumés avec le ticket FRONT-09

| Écart                                                                                                                                                          | Raison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Le garde-fou vit dans le preset `next` de `@repo/eslint-config`, pas dans les trois `eslint.config.mjs` que nomme la portée                                    | Trois copies du même arbitrage contredisent le mot d'ordre du ticket lui-même — « trois applications, une seule convention » — et divergent à la première correction. Les trois `eslint.config.mjs` sont inchangés ; une quatrième application serait gardée le jour de sa création.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Les zones sont **développées depuis le disque**, pas écrites                                                                                                   | `import-x/no-restricted-paths` ne sait pas exprimer « toutes les features sauf celle-ci » sans nommer les sœurs. Une liste à tenir se périme, ce que BACK-04b avait déjà refusé en préférant `containers = ["app.modules.*"]` à une énumération. `boundaries.js` développe `frontend/*/features/*/` à chaque chargement, comme `base.js` développe la liste des `tsconfig`.                                                                                                                                                                                                                                                                                                                                                              |
| Trois familles de zones là où le ticket en nomme deux                                                                                                          | La troisième — personne ne remonte vers `app/` — n'est pas du zèle, mais son motif a dû être corrigé en revue : la première famille ne se contourne pas, son `target` nommant déjà `app/`. C'est la **deuxième** que la troisième referme — sans elle, `components/` importerait une page, et une page importe légitimement une feature : le transverse dépendrait d'un domaine en deux sauts, `no-restricted-paths` ne voyant qu'une arête à la fois. S'y ajoute la raison de fond : `app/` est la racine de composition, et une racine ne se consomme pas.                                                                                                                                                                             |
| La surface publique d'une feature est la **racine de son dossier**, sans baril `index.ts`                                                                      | Le dépôt n'a aucun baril et l'a décidé en SHARED-03 ; `import-x/no-cycle` est en `error` ; et `proxy.ts` n'a besoin que d'**un** module de la feature `identity`, qu'un baril l'obligerait à atteindre par un fichier réexportant aussi `require-role.ts` et ses API serveur. La surface se déclare par un motif de chemin, comme le `"./components/*"` de `@repo/ui`. **La première rédaction invoquait le runtime Edge** — un baril y ferait entrer `next/headers`, absent de ce runtime : c'est faux depuis que Next 16 a renommé `middleware.ts` en `proxy.ts`, que le manifeste de build déclare en `"runtime": "nodejs"`. Constat de revue, motif corrigé partout où ce ticket l'avait écrit.                                      |
| `features/auth/` livrée sous le nom `features/identity/`                                                                                                       | Le ticket se contredit : sa structure cible écrit `auth`, sa règle de nommage dit « les noms suivent les modules backend ». `auth` n'est qu'un préfixe d'URL ; le module s'appelle `identity`, et c'est le nom du dossier qu'Orval produira. La règle l'emporte sur l'exemple.                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `features/scheduling/` non créée                                                                                                                               | Pas une ligne de planification côté client. Un dossier nommé d'après un sujet dont rien n'existe est la faute que le ticket combat, en pire : le `domain/entities/` plat écarté côté backend contenait au moins du code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `frontend-individual` reçoit un `features/` vide (`.gitkeep`), sans feature                                                                                    | L'application n'a aucun code de domaine — sa page d'accueil est une preuve de câblage. Le dossier est posé pour que le critère « les trois applications » ait un sens et que la première feature n'ait rien à décider, selon la convention déjà écrite au README : « les dossiers encore vides contiennent un `.gitkeep` […] chacun sera rempli par le ticket qui lui correspond ».                                                                                                                                                                                                                                                                                                                                                      |
| `features/health/` dans `frontend-professional`, alors que `health` n'est pas un module backend                                                                | C'est une étiquette OpenAPI, servie par `app/shared/infrastructure/api/health.py`. Mais c'est le **seul** fichier qu'Orval produise aujourd'hui en `tags-split`, et le composant qui le consomme n'a pas d'autre nom juste. La règle « la feature consomme le fichier de même nom » se vérifie donc littéralement sur lui.                                                                                                                                                                                                                                                                                                                                                                                                               |
| `lib/session.ts` reste dans `lib/` au lieu de descendre dans `features/identity/`                                                                              | C'est la frontière qui a répondu, pas nous. `components/navigation.ts` et `components/admin-sidebar.tsx` lisent son type `Role`, et la famille 2 interdit à `components/` d'importer une feature — **y compris un import de type**, vérifié. Le déplacer aurait fait échouer le lint le jour de sa pose. Ce fichier n'est pas l'intérieur d'un domaine : c'est le vocabulaire de session de l'application, que FRONT-07 fera monter dans `@repo/api-client`.                                                                                                                                                                                                                                                                             |
| `app/site-url.ts` déplacé en `lib/site-url.ts`, ce que le ticket ne demandait pas                                                                              | Application de sa propre règle — `app/` ne porte que du routage — et non une contrainte mécanique : la troisième famille de zones ne vise pas `app/` lui-même, un import `app/robots.ts` → `app/site-url.ts` n'aurait donc jamais échoué au lint. C'est un choix, pas une conséquence. `site-url.ts` n'est ni une route ni un fichier de métadonnées Next, c'est de la configuration que trois fichiers de routage partagent. Trois imports mis à jour, aucun changement de comportement — `robots.txt` et `sitemap.xml` restent prérendus.                                                                                                                                                                                              |
| Renommages en anglais hors du périmètre littéral                                                                                                               | `cliniques-table.tsx` → `clinics-table.tsx`, `CliniquesTable` → `ClinicsTable`, `Clinique` → `Clinic` et ses champs, `CliniquesPage` → `ClinicsPage`. Le champ `statut` devenant `status`, ses valeurs passent en anglais elles aussi (`ClinicStatus`) et une table `STATUS_LABELS` rend les libellés français — sans elle le même type aurait mêlé les deux langues, et l'écran reste identique au caractère près. Le dépôt écrit ses identifiants en anglais ; déplacer un fichier sans corriger son nom aurait figé l'écart dans un chemin neuf. Les libellés d'écran et les segments de route (`/cliniques`, `/utilisateurs`) restent en français : ce sont des textes vus par l'utilisateur.                                        |
| `Role` aligné sur le contrat de l'API : `'admin' \| 'professional' \| 'individual'`                                                                            | Les valeurs étaient `'professionnel'` et `'particulier'`, là où l'`AccountType` du module `identity` vaut `professional`, `individual`, `admin`. La comparaison aurait été **toujours fausse** le jour où le jeton serait réellement lu, sans qu'aucun typage ne le signale — les deux côtés étant des chaînes. Seule `'admin'` est comparée aujourd'hui : le changement est sans effet fonctionnel.                                                                                                                                                                                                                                                                                                                                     |
| Le critère « composants sans métier remontés dans `@repo/ui` » est tenu par **vérification**, sans aucun déplacement                                           | Les trois composants restés dans `components/` portent tous une connaissance de l'application — la liste des sections, les rôles, le chemin courant. Aucun n'est générique, donc aucun n'avait à remonter. C'est un résultat, pas un oubli, et il vaut d'être écrit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| La preuve est un programme Node, pas une suite de tests                                                                                                        | Aucun runner frontend n'existe : Vitest est le périmètre de FRONT-06, la CI complète celui de QA-02. Même arbitrage et même forme que le `verify-query-keys.ts` de FRONT-04, branché sur le script `test` du package.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Une arborescence de démonstration de six fichiers dans `packages/config-eslint/fixtures/`                                                                      | `no-restricted-paths` ne se déclenche que si l'import **se résout sur le disque**. Aucune feature réelle n'ayant encore de sous-dossier, la famille 1 n'aurait eu aucun import à voir : elle serait restée non prouvée. Les huit autres contrôles tournent, eux, sur le vrai dépôt avec la configuration réelle des applications.                                                                                                                                                                                                                                                                                                                                                                                                        |
| `.github/workflows/ci-frontend.yml` créé alors que QA-02 porte la CI frontend                                                                                  | Précédent explicite de BACK-04b, qui a créé `ci-backend.yml` avant QA-01, au motif qu'un garde-fou qui ne tourne que sur le poste de celui qui écrit le code n'est pas un garde-fou. Le fichier ne fait tourner que ce que ce ticket livre ; QA-02 y ajoutera le lint, le typage, les tests et le build. La symétrie n'est toutefois pas parfaite, et autant le dire : `lint-imports` était une commande à part que QA-01 aurait dû ajouter explicitement, alors que la preuve livrée ici est branchée sur le script `test` du package — donc ramassée d'office par l'étape « tests » de QA-02. Ce workflow est plus redondant avec son successeur que ne l'était `ci-backend.yml` ; ce qui le justifie reste que QA-02 n'a pas de date. |
| Deux scopes de commit ajoutés à `commitlint.config.mjs`                                                                                                        | `config-eslint` et `config-prettier` existent depuis SETUP-03 sans avoir jamais eu leur entrée : un `feat(config-eslint): …` était refusé par le hook `commit-msg`. La règle du fichier — « un nouveau workspace ajoute son scope ici » — vaut aussi rétroactivement. `api-client`, absent de la liste recopiée dans `conventions-du-depot.md`, y est remis au passage.                                                                                                                                                                                                                                                                                                                                                                  |
| L'import croisé entre features reste **autorisé** par la surface publique, alors que le garde-fou du ticket dit « interdisant l'import croisé entre features » | C'est la seule divergence où le livré est plus **permissif** que la lettre du ticket, et elle mérite donc d'être ici plutôt que seulement dans les alternatives écartées de l'ADR. Le ticket se contredit lui-même : ses règles disent qu'une feature « n'importe JAMAIS l'**intérieur** d'une autre : elle passe par ce que celle-ci exporte ». C'est cette phrase-là qui a été suivie. L'indépendance totale, à la manière du contrat 3 du backend, aurait forcé le premier besoin partagé — un écran de rendez-vous qui affiche la fiche de la clinique — dans une couche transverse artificielle.                                                                                                                                    |
| Les espaces transverses sont **découverts**, non énumérés                                                                                                      | Constat de revue : la première rédaction listait `components` et `lib` en dur, si bien qu'un `hooks/` créé demain n'aurait été gardé par rien — mesuré, trois imports interdits y passaient sans un mot. Tout dossier de premier niveau qui n'est ni `app/` ni `features/` est donc traité comme transverse. Le monde est fermé, comme l'`exhaustive = true` des contrats de couches du backend.                                                                                                                                                                                                                                                                                                                                         |
| `packages/config-eslint/fixtures/tsconfig.json` ajouté à un package qui n'avait pas de `tsconfig`                                                              | Il ne sert qu'au résolveur de l'arborescence de démonstration, et vit deux niveaux sous `packages/`, hors du motif `packages/*/tsconfig.json` de `base.js` : il n'entre dans la résolution d'aucun workspace. La phrase de `configurations-partagees.md` que cela rendait fausse est précisée.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `frontend-professional` reste sans `lib/`, là où les deux autres en ont un                                                                                     | Le modèle décrit quatre espaces ; cette application n'a rien de transverse à y ranger. Poser un `lib/.gitkeep` pour la symétrie aurait été la structure fictive refusée plus haut pour `features/scheduling/` — la convention du `.gitkeep` a servi pour `features/`, que les critères d'acceptation nomment explicitement, et pas au-delà.                                                                                                                                                                                                                                                                                                                                                                                              |
| Mentions périmées de `middleware.ts` corrigées dans les fichiers touchés                                                                                       | Next 16 a renommé la convention en `proxy.ts` dès FRONT-03, mais cinq commentaires disaient encore « le middleware ». Les trois qui vivent dans des fichiers que ce ticket déplace ou modifie sont corrigés ; ceux d'`app/robots.ts`, `app/layout.tsx`, `app/(auth)/login/page.tsx` et `next.config.ts` restent, faute d'y toucher par ailleurs.                                                                                                                                                                                                                                                                                                                                                                                         |
| La « couche d'enveloppement » que l'ADR-0027 attribue à FRONT-09 n'est pas livrée                                                                              | L'[ADR-0027](../adr/0027-portee-des-cles-de-cache.md) nomme deux fois ce ticket comme celui qui apporterait une couche d'enveloppement du client généré, et le typage que la clé nue perd. Celui-ci ne porte que la structure et sa frontière : l'enveloppement suivra les premiers écrans qui consomment vraiment l'API, FRONT-19 en tête. Un ADR ne se réécrit pas, la précision se consigne donc ici.                                                                                                                                                                                                                                                                                                                                 |
| La justification Tailwind de FRONT-01 corrigée par la mesure                                                                                                   | La ligne `@source '../features/**'` est ajoutée aux trois applications, mais **pas** pour la raison annoncée. Mesuré : construire `frontend-admin` avec son `globals.css` privé de ses trois `@source` produit un CSS qui contient encore les classes de `app/` **et** de `features/` — la détection automatique de Tailwind v4 balaie déjà le dossier de l'application. Les lignes restent parce qu'une énumération explicite vaut mieux qu'un comportement implicite, et parce qu'une énumération incomplète serait le pire des trois états. La phrase de FRONT-01 est réécrite là où elle vivait.                                                                                                                                     |

:::note Ce que le garde-fou ne saura jamais faire

Trois choses lui échappent par construction, et la page
[Anti-patterns](../architecture/anti-patterns.md) les inscrit en colonne **Revue** : un composant
chargé de métier rangé dans `components/` sous l'étiquette « transverse » — aucun outil ne juge de
la présence de métier ; une logique recopiée d'une feature à l'autre plutôt qu'importée ; une
frontière franchie par un appel réseau plutôt que par un `import`.

:::
