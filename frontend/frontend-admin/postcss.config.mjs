// Chaine PostCSS de l'application : celle du depot, re-exportee (SHARED-02).
//
// Rien a configurer ici. Tailwind v4 passe entierement par le plugin
// `@tailwindcss/postcss`, et le theme vit dans `@repo/tailwind-config`.
export { default } from '@repo/tailwind-config/postcss.config';
